# macaway

An attempt at making a macro recorder and player for wayland

# Install

```sh
nix profile install github:Elec3137/macaway
```
note: this will compile the program if it cannot find it in your configured binary caches

